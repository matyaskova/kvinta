function zmena(element,barva) {
    element.style.color = barva
  }
function zvetsi(element,size) {
    element.style.fontSize = size
}  

function zarovka(objekt,on_or_off ){
    objekt.src = on_or_off +".png"

}